{
  "phases": {
    "setup": {
      "nixPkgs": ["python312"],
      "aptPkgs": []
    },
    "install": {
      "cmds": ["pip install -r requirements.txt"]
    }
  },
  "start": {
    "cmd": "uvicorn api.simple_api:app --host 0.0.0.0 --port $PORT"
  }
}
